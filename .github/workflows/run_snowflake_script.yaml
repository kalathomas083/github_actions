name: Create New Schema

on:  push


jobs:
    test_job:
      runs-on: ubuntu-latest
      environment: Migrate
  
      steps:
        - name: Checkout Repository
          uses: actions/checkout@v2
  
        - name: Print Message
          run: echo "${{ secrets.SF_PASSWORD }}"

        - name: Check Python
          run: |
            # the_password=${{ secrets.SF_PASSWORD }}
            the_password="Test"
            echo $the_password
            OUTPUT_VAR=$(python3 .github/src/test_script.py $the_password)
            echo $OUTPUT_VAR

