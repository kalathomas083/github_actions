name: Create New Schema

on:  push


jobs:
    test_job:
      runs-on: ubuntu-latest
      environment: Migrate
  
      steps:
        - name: Checkout Repository
          uses: actions/checkout@v2
  
        - name: Print Message
          run: echo "${{ secrets.SF_PASSWORD }}"

        - name: Check Python
          run: |
            OUTPUT_VAR=$(python3 .github\src\test_script.py ${{ secrets.SF_PASSWORD }})
            echo $OUTPUT_VAR

